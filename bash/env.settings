PATH=$HOME/.local/bin:$PATH

isPathCmd vim   && declare -x EDITOR=$(cmdPath vim)
isPathCmd nvim  && declare -x EDITOR=$(cmdPath nvim)
isPathCmd less  && declare -x PAGER=$(cmdPath less)

declare -x XDG_CONFIG_HOME=$HOME/.config

# vim: ft=sh
