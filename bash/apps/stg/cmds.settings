minimak () { (
  reveal "$FUNCNAME"
  cd "$HOME"/dotfiles
  stg pop qwerty
  cd "$HOME"/.vim
  stg pop  qwerty
) }

qwerty () { (
  reveal "$FUNCNAME"
  cd "$HOME"/dotfiles
  stg push qwerty
  cd "$HOME"/.vim
  stg push qwerty
) }

# vim: ft=sh
